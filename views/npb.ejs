<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2><%= mes %></h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.team_id %></td>
            <td><%= row.チーム名 %></td>
            <td><%= row.所在地 %></td>
            <td><%= row.本拠地 %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>
